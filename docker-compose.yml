services:
  web:
    build: .
    image: piro25-cardgame-3
    container_name: piro25-cardgame-3
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
      - ./staticfiles:/app/staticfiles
    restart: unless-stopped